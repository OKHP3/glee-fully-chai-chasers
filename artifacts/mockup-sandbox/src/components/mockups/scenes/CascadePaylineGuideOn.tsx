import { SceneFrame } from "./_SceneFrame";

/** #40 — Cascade payline guide ON: all 40 payline paths have is-guide class (faint amber lines) */
export default function CascadePaylineGuideOn() {
  return (
    <SceneFrame
      file="cascade-payline-guide-on.html"
      title="Cascade – Payline Guide On (#40)"
    />
  );
}
