import { SceneFrame } from "./_SceneFrame";

/** #30 — Win status line only: sub-threshold win shown as marquee "+248 coins" with no overlay */
export default function WinStatusOnly() {
  return (
    <SceneFrame
      file="win-status-only.html"
      title="Win – Status Line Only, No Overlay (#30)"
    />
  );
}
