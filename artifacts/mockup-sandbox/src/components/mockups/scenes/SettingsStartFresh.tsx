import { SceneFrame } from "./_SceneFrame";

/** #24 — Settings: Start Fresh confirmation modal over dimmed settings page */
export default function SettingsStartFresh() {
  return (
    <SceneFrame
      file="settings-start-fresh.html"
      title="Settings – Start Fresh Confirmation (#24)"
    />
  );
}
