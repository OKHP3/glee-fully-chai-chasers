import { SceneFrame } from "./_SceneFrame";

/** #21 — Settings sub-tab: Reduce Motion toggled On, data-reduced-motion="true" on root */
export default function SettingsReduceMotion() {
  return (
    <SceneFrame
      file="settings-reduce-motion.html"
      title="Settings – Reduce Motion (#21)"
    />
  );
}
