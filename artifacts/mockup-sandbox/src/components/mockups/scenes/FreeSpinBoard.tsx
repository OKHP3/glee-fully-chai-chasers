import { SceneFrame } from "./_SceneFrame";

/** #43 — Generic free-spin cabinet baseline (Standard Chai Chase, heading / spin counter / reel grid) */
export default function FreeSpinBoard() {
  return (
    <SceneFrame
      file="free-spin-board.html"
      title="Free Spin Board — Generic Baseline (#43)"
    />
  );
}
