import { SceneFrame } from "./_SceneFrame";

/** #57 — Treat Jar Bonus free-spin cabinet (TREAT JAR BONUS! title, caller-label override) */
export default function TreatJarFreeSpins() {
  return (
    <SceneFrame
      file="treat-jar-free-spins.html"
      title="Treat Jar Bonus Free Spins (#57)"
    />
  );
}
