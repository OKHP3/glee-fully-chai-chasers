import { SceneFrame } from "./_SceneFrame";

/** #18 — Settings page: full overlay with Look & Feel, Sound, Reduce Motion, Payline Guide, About, Start Fresh */
export default function SettingsPage() {
  return (
    <SceneFrame
      file="settings-page.html"
      title="Settings Page (#18)"
    />
  );
}
