import { SceneFrame } from "./_SceneFrame";

/** #52 — Treat Time main board (hand-toss wild animation on primary reel grid, treat-time-main-mode) */
export default function TreatTimeMainBoard() {
  return (
    <SceneFrame
      file="treat-time-main-board.html"
      title="Treat Time Main Board (#52)"
    />
  );
}
