import { SceneFrame } from "./_SceneFrame";

/** #20 — Settings sub-tab: Sound (On, music volume 70%, SFX volume 80%, preview button active) */
export default function SettingsSound() {
  return (
    <SceneFrame
      file="settings-sound.html"
      title="Settings – Sound (#20)"
    />
  );
}
