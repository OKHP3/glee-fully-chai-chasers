import { SceneFrame } from "./_SceneFrame";

/** #41 — Cascade payline guide OFF: payline paths have no is-guide (default invisible state) */
export default function CascadePaylineGuideOff() {
  return (
    <SceneFrame
      file="cascade-payline-guide-off.html"
      title="Cascade – Payline Guide Off / Default (#41)"
    />
  );
}
