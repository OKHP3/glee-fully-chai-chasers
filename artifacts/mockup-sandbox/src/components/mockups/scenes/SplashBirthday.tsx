import { SceneFrame } from "./_SceneFrame";

/** #2 — Birthday-window splash variant: birthday panel, +10,000 coins grant, bday CTA */
export default function SplashBirthday() {
  return (
    <SceneFrame
      file="splash-birthday.html"
      title="Splash – Birthday Variant (#2)"
    />
  );
}
