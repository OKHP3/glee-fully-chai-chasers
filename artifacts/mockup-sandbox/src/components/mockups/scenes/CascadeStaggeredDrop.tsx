import { SceneFrame } from "./_SceneFrame";

/** #37 — Cascade step 1+: cells have beam-drop with staggered --drop-delay per reel/row */
export default function CascadeStaggeredDrop() {
  return (
    <SceneFrame
      file="cascade-staggered-drop.html"
      title="Cascade – Staggered Beam-Drop (#37)"
    />
  );
}
