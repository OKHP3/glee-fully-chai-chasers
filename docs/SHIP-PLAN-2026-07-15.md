# SHIP PLAN — Final Three Windows Before 07/17
**Author:** Claude (PM) · **Date:** 2026-07-15 · **Independently verified against HEAD, not taken from handoff claims**

## 1. Verified current state (I ran everything myself)

| Claim | Verified reality |
|---|---|
| Math | **ORACLE FULLY GREEN.** 200k-spin seeded sim: RTP **95.91%** (band 95.5-96.5), win 1-in-3.16, free spins 1-in-32, 8+ mega 1-in-621, UniGlee 1-in-417, cat pop-in 1-in-32.3. Oracle unmodified and unweakened. The README's "intentionally failing at 93.54%" is **stale** — a later retune fixed it |
| Tests / build | 14 files, **82 tests, all passing**; tsc clean; vite build clean; bundle JS 90.8KB gzip 26.4KB |
| Art | Production-grade. Symbol atlas, splash, Joey/Phoebe wheel and wilds art meet or exceed the DESIGN-SPEC §11 bar and the S18 ruling. This is the gift-worthy look |
| Governance | Coherent through S26; IMPLEMENTATION-BASELINE.md layered-authority model is sound; S17 narrative policy intact everywhere I grepped |
| Deploy | Pages workflow present; deploy gates passing (tree clean of private folders) |

The realignment worked. Codex's build, on top of Replit's engine round and the original spec skeleton, is the best version of this product so far. Credit where due — and the layered-authority baseline doc is genuinely good governance.

## 2. Gaps — needed, missing, at-risk

### P0 — the gift fails without these (Window 1: tonight)

1. **Birthday Reveal does not exist in `src/`.** The single most important 20 seconds of the product: date-gated first-launch sequence, `BIRTHDAY_MESSAGE` constant (Jamie's own words, nobody else writes it), skippable, re-watchable. Small bounded build (~1 screen + date flag in state). Owner: Codex or Claude, one tool, tonight.
2. **Asset weight: ~10MB of PNGs** (atlas 2.6MB, splash 2.3MB, wilds 2.0MB, wheel 1.9MB, handbag 1.3MB). First load on cellular will feel broken. Convert display copies to WebP at ~80 quality (expect ~70-80% reduction, zero visual loss at phone sizes), keep PNG masters in repo. Mechanical, low-risk, huge UX win. Owner: any tool, tonight.
3. **README engineering-status staleness** (math "failing" when it's green; feature list drift). Undermines trust at reveal when she or anyone else reads the repo. 15-minute fix. (Stale math line corrected by Claude 2026-07-15; feature-list pass still owed.)

### P1 — strongly wanted, fit in Window 2 (Thursday daytime)

4. **UniGlee marathon (S22) — scope discipline required.** Full multi-chapter 100-500-spin marathon with pause/resume/fast-mode is NOT a one-day feature. Ship **v1: UniGlee awards a flat 100 free spins played in the existing free-spin flow with the takeover art**, RTP-checked by the oracle (Claude-owned math). Chapters, variety, pause/resume ship *after* the birthday as "the game keeps unwrapping." Do not let this feature eat Thursday.
5. **Daily Bonus Wheel** — small, high retention value for the days after the 17th.
6. **Music loops** (base + free-spin beds; one-shots exist today). S23 wants distinct motifs; even one looped base bed transforms feel. Timebox: half a day max, Web Audio sequenced, no samples.
7. **Twelve-mechanics cleanup (baseline assigns to Claude):** remove legacy level-12 bet unlock and 12-cap Treat Jar (S21), re-run oracle. Small, but it's a settled ruling and currently unimplemented.

### P2 — cut without guilt (post-birthday gifts)

Bold Chai keepsake pick shelf, milestone scenes + collection shelf, chapter-specific bonus presentation, final music stems/mix, device-regression gallery, service worker/offline (manifest-only PWA still installs to home screen and runs fine online — offline is polish, not gift-critical).

### Ship-time obligations (Window 3, non-code)

- **History purge** (private-work/photo-triage.md): reference-photos + attached_assets out of git history via filter-repo, then force-push. Run ONLY after all tools stop committing — purge last, after the code freeze.
- **Embargo lift prep:** content/ drafts get final play link + screenshots; publish after she has the gift.
- **On-device rehearsal:** install to her-model iPhone from Pages URL, full loop: splash → spin → free spins → Treat Time → Doorbell Panic → Bold Chai → reveal. Sound on AND muted. Reduced motion on.

## 3. The three windows

**Window 1 — tonight (Wed):** Birthday Reveal built and date-tested (fake the clock to verify) · WebP conversion + path swap · README truth pass · quick iPhone smoke test. *Freeze nothing yet; this is the last feature-building night for P0s.*

**Window 2 — Thursday daytime:** UniGlee marathon v1 (flat 100 spins, oracle green) · Daily Bonus Wheel · one music bed if time remains · twelve-cleanup + retune. Hard stop on anything not started by mid-afternoon.

**Window 3 — Thursday night: FREEZE, not features.** Code freeze at start of window. Then: full on-device QA (twice), fix only reveal-blocking bugs, history purge + force-push, final deploy verification from a clean phone, Jamie writes `BIRTHDAY_MESSAGE`, home-screen icon staged on Glee's phone if he can get it, embargo content staged. A boring Thursday night is a successful Thursday night.

## 4. Risks

- **Marathon scope creep** is the #1 threat to Thursday. The v1 cut is already decided above; hold it.
- **Last-minute cross-tool collisions:** one tool per window per file area, per IMPLEMENTATION-BASELINE §1. Codex owns reveal UI if Claude owns twelve-cleanup math, or vice versa — never both in `board.ts` the same night.
- **Purge is destructive and final:** run it exactly once, after freeze, with all agents stopped, from a fresh clone check. The command and verification grep are in private-work/photo-triage.md.
- **The oracle stays the gate:** any Thursday engine change (UniGlee v1, twelve-cleanup) ships only with all six gates green. No exceptions on deadline week — that was the whole point of building the oracle.
